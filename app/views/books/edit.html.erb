<% if @book.errors.any? %>
<%= @book.errors.count %>errors pronibited this from being saved:
<ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>

<div class="edit-book">
<h4>Body</h4>
</div>

<div class="edit-book">
<%= f.text_area :body %>
</div>

<div class="edit-book">
<%= f.submit 'Update Book' %>
</div>
<% end %>

<%= link_to 'Show', @book %>
<%= "|" %>
<%= link_to 'Back', books_path %>
